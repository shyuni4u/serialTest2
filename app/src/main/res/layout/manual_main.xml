<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@color/black_95">

    <androidx.viewpager.widget.ViewPager
            android:id="@+id/manual_viewpager"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />

    <com.google.android.material.tabs.TabLayout
            android:id="@+id/manual_tab_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:tabIndicator="@drawable/tab_indicator_dot_black_manual"
            app:tabBackground="@drawable/tab_amber_dot_manual"
            app:tabGravity="center"
            app:tabIndicatorColor="@color/dot_selected"
            app:tabIndicatorGravity="center"
            app:tabMaxWidth="20dp" />

</LinearLayout>